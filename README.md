# minimal-webchat

A simple webchat using socket.h library from C/Unix

## Build

`make clean` <br>
`make`

## Usage

### Server-side

`./server <ip_address> <port>`

### Client-side

`./cliente <ip_address> <port>`
